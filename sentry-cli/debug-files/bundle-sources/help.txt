sentry-cli-debug-files-bundle-sources 
Create a source bundle for a given debug information file

USAGE:
    sentry-cli debug-files bundle-sources [OPTIONS] <paths>...

ARGS:
    <paths>...    The path to the input debug info files.

OPTIONS:
        --auth-token <AUTH_TOKEN>    Use the given Sentry auth token.
    -h, --help                       Print help information
        --header <KEY:VALUE>         Custom headers that should be attached to all requests
                                     in key:value format.
        --log-level <LOG_LEVEL>      Set the log output verbosity. [possible values: trace, debug,
                                     info, warn, error]
    -o, --output <PATH>              The path to the output folder.  If not provided the file is
                                     placed next to the input file.
        --quiet                      Do not print any output while preserving correct exit code.
                                     This flag is currently implemented only for selected
                                     subcommands. [aliases: silent]
